# Operational task state — 994029

PROMPT_ID: 994029
Updated: 2026-09-24 15:55 Europe/Copenhagen

## Objective
Complete only the ActivityWatch/Kuma runtime cutover from 620949.

## Constraints
- `fedora-system-monitor` is read-only for this task while 966124 owns writes.
- Mutations are limited to `activity-watch-uploader` and its Fedora ActivityWatch units until 966124 releases that ownership.
- No broad Kuma/repository audit; no duplicate central monitor.

## Plan / checklist
- [x] Read canonical prompt/global constraints and claim 994029.
- [x] Verify isolated task worktree starts at merged main `b8ef359` and is clean.
- [x] Run targeted tests + py_compile.
- [x] Fast-forward canonical Fedora checkout to `b8ef359` and deploy existing user units.
- [x] Verify timer/service/runtime and absence of proprietary Kuma credential dependency.
- [x] Run one controlled ActivityWatch job successfully.
- [x] Read-only verify central monitor freshness and legacy-producer behavior.
- [ ] Remove/disable the stale legacy Kuma monitor after 966124 releases `fedora-system-monitor` ownership, then verify central uniqueness/freshness.
- [ ] Finalize roadmap PASS and checkpoint terminal evidence.

## Current step
Park 994029 at the legacy-monitor cleanup gate while 966124 owns `fedora-system-monitor`; continue 966124.

## Verified facts
Source gates PASS: 17/17 tests + py_compile + diff-check. Runtime checkout is `b8ef359`; timer is enabled+active; service Result=success/exit 0; deployed unit/env contain no KUMA push URL/token dependency. Controlled run at 15:51 local succeeded and advanced activity-watch-data. Central monitor ID 59 is active and received fresh UP heartbeats; legacy ID 45 stopped receiving heartbeats after the cutover run but remains active in Kuma.

## Decisions
Do not mutate `fedora-system-monitor` or its central Kuma control-plane path while 966124 owns it. Legacy producer is gone; only legacy monitor cleanup remains.

## Completed
Canonical-state readback, claim, source gates, runtime fast-forward/deploy, timer/service readback, controlled run, central read-only verification.

## Remaining
Disable/remove legacy Kuma monitor ID 45 through the central controller after 966124 completes; prove ID 59 is the only uploader monitor; finalize.

## Blockers
Temporary ownership gate only: the old monitor `ActivityWatch Git Uploader (Fedora)` (ID 45) is still active even though its last heartbeat predates the new controlled run. 966124 currently owns `fedora-system-monitor`.

## Evidence
Roadmap issue #1059; `b8ef359`; 17 tests PASS; service run `20260924T135142Z` success; data repo HEAD advanced to `a73a9ba...`; Kuma ID 59 fresh UP at 13:53 UTC, legacy ID 45 last heartbeat 13:45 UTC and no heartbeat from the 13:51 controlled run.

## Acceptance criteria
ActivityWatch runtime works without proprietary Kuma producer/credentials and one existing central monitor is fresh with no duplicate/coverage gap.

## Next action
Complete 966124. Then clean up legacy Kuma monitor ID 45 using the canonical central control-plane path, read back IDs 45/59, and finalize 994029 if only ID 59 remains active/fresh.
