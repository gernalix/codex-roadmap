# Spiegazioni della roadmap

[[README|README]] · [[roadmap|Roadmap]]

Qui trovi, in parole semplici, **perché ogni task richiede ancora Codex e se conviene lanciarlo come Prompt normale oppure Goal**. Tutto ciò che era correggibile direttamente sul remoto è stato tolto dalla coda.

|   # | Prompt | Spiegazioni | Livello ragionamento | Tipo prompt |
| --: | ------ | ----------- | -------------------- | ----------- |
| 1 | [[prompts/codex-usage-529184-goal-recovery-backfill]] | Il fix è già nel repo remoto. `978216` ora fa una sola sync bounded, deploy con `--skip-fetch` e avvia **solo** `codex_usage_publisher.py`, non l'intero service che trascinerebbe chat-dump publisher e GitHub Actions watcher. Se `529184` è già corretto salta anche la ripubblicazione. L'acceptance verifica separatamente commit/runtime, tutti i campi esatti del record recuperato e stato pulito/sincronizzato di `codex-usage`. **Prompt**, GPT-5.5/low: pipeline breve, deterministica e idempotente. | low | Prompt |
| 2 | [[prompts/fedora-runtime-validation]] | I fix per user-systemd, Telegram e Kuma sono già su `main` e la CI remota è PASS. Codex serve solo perché deve fare un unico deploy sul Fedora reale e provare user bus, helper Telegram installato e sessione Kuma/config locale reali. I vecchi due task Fedora sono accorpati qui. **Prompt**, GPT-5.5/medium: scope stretto, ma un failure runtime può richiedere una diagnosi leaf. | medium | Prompt |
| 3 | [[prompts/personalhub-release-apk-minification]] | Il bug Random Timer è già corretto remotamente; non serve più una fase di implementazione separata. Codex deve eseguire il test mirato, costruire una sola release shrunk, provarla sul Pixel includendo reboot/restore del Random Timer e consegnare esattamente lo stesso APK. **Prompt**, GPT-5.5/low: pipeline finale deterministica; niente emulatore o audit generale. | low | Prompt |
| 4 | [[prompts/logseq-updates-pat-safety-closure]] | Il PAT è già rimosso dal tree corrente e lo updater è già stato irrobustito; resta il problema che solo un ambiente Git autenticato locale può chiudere in sicurezza: rewrite della history, verifica gitleaks all-refs e stato credential senza esporre il secret. **Prompt**, GPT-5.6 Sol/medium + STRICT per il rischio sicurezza/force-push. | medium | Prompt |
| 5 | [[prompts/codex-usage-monitor-capsule-boundaries]] | È un refactor architetturale che deve preservare entrypoint systemd e packaging, quindi l'acceptance richiede anche deploy e smoke sul Fedora reale. **Goal**, GPT-5.6 Sol/medium: la complessità è davvero trasversale e riguarda boundary/import/runtime, non semplice editing. | medium | Goal |
| 6 | [[prompts/megavault-internal-capsule-boundaries]] | Il refactor deve eliminare wildcard/re-export/monkey-patching senza alterare il comportamento sul `megavault.sqlite` canonico e deve passare `megavault.py validate` sul DB privato locale. **Goal**, GPT-5.6 Sol/medium + STRICT: infrastruttura centrale e cambi architetturali con verifica sul dato reale. | medium | Goal |
