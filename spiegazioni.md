# Spiegazioni della roadmap

[[README|README]] · [[roadmap|Roadmap]]

Qui trovi, in parole semplici, **perché ogni task richiede ancora Codex e se conviene lanciarlo come Prompt normale oppure Goal**. Tutto ciò che era correggibile direttamente sul remoto è stato tolto dalla coda.

|   # | Prompt | Spiegazioni | Livello ragionamento | Tipo prompt |
| --: | ------ | ----------- | -------------------- | ----------- |
| 1 | [[prompts/adb-device-keeper-canonicalize]] | La sorgente canonica `gernalix/adb-device-keeper`, i test, la CI, il deploy helper e la rimozione della logica WhatsApp sono già completati sul remoto. Resta solo ciò che richiede il Fedora reale: sincronizzare il checkout, distribuire la release, verificare user-systemd e fare un unico disconnect/reconnect del Pixel 8a via Tailscale. **Prompt**, GPT-5.5/low + FAST: è un gate runtime locale lineare, senza nuova implementazione. | low | Prompt |
| 2 | [[prompts/codex-usage-publisher-cycle-path-deploy]] | I fix remoti sono già implementati e testati: path cycle-aware, parser degli stati terminali con `FIXED` e analisi prompt continuation-safe. Codex serve solo per il deploy sul Fedora reale, un singolo backfill/push del repo privato `codex-usage` e la verifica dei casi `583214` e `742615` (`UNKNOWN` → `FIXED`). **Prompt**, GPT-5.5/low + FAST: lavoro locale lineare, senza nuova implementazione o audit. | low | Prompt |
| 3 | [[prompts/fedora-runtime-validation]] | `847392` ha già verificato sul Fedora reale user-systemd e Telegram; inoltre i gap CI/deploy sono già corretti sul remoto e la CI è verde. Resta solo ciò che richiede lo stato live locale: deploy dell'ultimo `main`, sessione autenticata del profilo Chrome canonico e readback Kuma #39/#40 + inversione Storage. **Prompt**, GPT-5.5/low + FAST: nessuna nuova implementazione è ammessa e al primo auth blocker deve fermarsi. | low | Prompt |
| 4 | [[prompts/logseq-updates-pat-safety-closure]] | Il PAT è già rimosso dal tree corrente e lo updater è già stato irrobustito; resta il problema che solo un ambiente Git autenticato locale può chiudere in sicurezza: rewrite della history, verifica gitleaks all-refs e stato credential senza esporre il secret. **Prompt**, GPT-5.6 Sol/medium + STRICT per il rischio sicurezza/force-push. | medium | Prompt |
| 5 | [[prompts/megavault-internal-capsule-boundaries]] | L'anti-pattern è ancora presente sul remoto (`import *`, re-export via `globals()` e monkey-patching). Il refactor deve preservare il comportamento sul `megavault.sqlite` canonico e passare `megavault.py validate` sul DB privato locale, ma non richiede persistenza multi-turn. **Prompt**, GPT-5.6 Sol/medium + STRICT: infrastruttura centrale e modifica architetturale ad alto rischio, però con scope e gate già pre-localizzati. | medium | Prompt |
| 6 | [[prompts/retained-repositories-github-ci-completion]] | L'handoff locale di MegaVault definisce già l'elenco canonico dei repository mantenuti; resta da lavorare sui checkout reali, modificare più repo e verificare i run GitHub senza rifare inventory o audit sicurezza. **Goal**, GPT-5.5/medium + STANDARD: è una campagna multi-repo seriale abbastanza lunga da beneficiare davvero della persistenza tra turni; i repo vengono chiusi uno alla volta e non riaperti dopo PASS. | medium | Goal |
